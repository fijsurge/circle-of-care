VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Cronofy — unified calendar OAuth (Google, Outlook, iCloud)
# Get credentials at: https://app.cronofy.com/developers
VITE_CRONOFY_CLIENT_ID=       # get from app.cronofy.com/developers
VITE_CRONOFY_REDIRECT_URI=    # e.g. https://yourdomain.com/calendar

# SendGrid — morning digest emails
# Store as Firebase secret (NOT here): firebase functions:secrets:set SENDGRID_API_KEY
# Store as Firebase secret:            firebase functions:secrets:set DIGEST_FROM_EMAIL

# Dev tools — enables the 🔧 DevPanel (only for the matching Firebase UID)
VITE_DEV_UID=          # Your Firebase UID — keep private, do not commit
